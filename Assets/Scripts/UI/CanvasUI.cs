using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class CanvasUI : MonoBehaviour
{
    /*public MessageUI message;
    public List<MessageUI> messages;
    private int messagesCount;
    public void SendMessage(string message,Color color){
        MessageUI newMessage = Instantiate<MessageUI>(message);
        messages.Add(newMessage);
        if(messages.Count > 1){
            newMessage.goTime = false;
        }
        newMessage.pos = new Vector2(150, (screen.height - message.RT.sizeDelta.y * 2));
    }
    public void CalculateMessages(){
        messages[0].goTime = true;
    }*/
}
